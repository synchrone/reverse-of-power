<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="160dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp"
    android:layout_marginHorizontal="12dp"
    android:clipChildren="false"
    android:clipToPadding="false">

    <!-- Photo container with glow layers -->
    <FrameLayout
        android:layout_width="140dp"
        android:layout_height="140dp"
        android:clipChildren="false"
        android:clipToPadding="false">

        <!-- Yellow spotlight behind photo (hidden until selected) -->
        <View
            android:id="@+id/targetSpotlight"
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:layout_gravity="center"
            android:background="@drawable/target_spotlight"
            android:visibility="gone" />

        <!-- Glowing ring around photo (hidden until selected) -->
        <View
            android:id="@+id/targetGlowRing"
            android:layout_width="136dp"
            android:layout_height="136dp"
            android:layout_gravity="center"
            android:background="@drawable/target_glow_ring"
            android:visibility="gone" />

        <!-- Player avatar photo in circle -->
        <ImageView
            android:id="@+id/targetPhoto"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_gravity="center"
            android:scaleType="centerCrop"
            android:background="@drawable/circle_background"
            android:clipToOutline="true"
            tools:src="@mipmap/ic_launcher" />

    </FrameLayout>

    <TextView
        android:id="@+id/targetName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@android:color/white"
        android:shadowColor="#80000000"
        android:shadowDx="2"
        android:shadowDy="2"
        android:shadowRadius="3"
        android:gravity="center"
        android:layout_marginTop="12dp"
        tools:text="Player 1" />

</LinearLayout>
